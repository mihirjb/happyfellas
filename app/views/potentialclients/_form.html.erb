<%= form_for(@potentialclient) do |f| %>
  <% if @potentialclient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@potentialclient.errors.count, "error") %> prohibited this potentialclient from being saved:</h2>

      <ul>
      <% @potentialclient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :fullname %>
    <%= f.text_field :fullname %>
  </div>
  <div class="field">
    <%= f.label :worktype %>
    <%= f.text_field :worktype %>
  </div>
  <div class="field">
    <%= f.label :budget %>
    <%= f.text_field :budget %>
  </div>
	<br/>
  <div class="actions">
    <%= f.submit "Add", :class =>"btn btn-primary btn-lg" %>
  </div>
<% end %>
